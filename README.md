In order to run the playbook (firewalld and postgres related tasks) you need to install the collections from the requirenments.yml using the command:  
ansible-galaxy collection install -r requirements.yml  
